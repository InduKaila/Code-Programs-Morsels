
public class ConvertFirstLetterToUpperCase {
	public static String converter(String userSource) {
		
		
		String newString = "";
		{
		
		for (int index = 0; index < userSource.length(); index++) {
			//newString = newString + userSource.charAt(index);
	
				if (userSource.charAt(index) == 0) {
					userSource.toUpperCase();				
				//}
				
		return newString = newString + userSource.charAt(index);
				}
		else {(return userSource.toLowerCase(index));
		}
		
	
	public static void main(String[] args) {
		System.out.println(converter("hello"));
	}
}
